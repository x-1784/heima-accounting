# 黑马记账 — 项目文档

## 项目简介

「黑马记账」是一个个人记账桌面应用，帮助用户记录和管理日常花销（人民币）。应用支持 Windows 和 Mac 平台，数据存储在用户本地电脑上，无需联网。

- **项目名称**：黑马记账 (Heima Expense Tracker)
- **创建日期**：2026-07-11
- **目标平台**：Windows + macOS

---

## 技术栈

| 层面 | 选型 | 说明 |
|------|------|------|
| 桌面框架 | Electron | 用网页技术构建桌面应用，最成熟的跨平台桌面方案 |
| 前端框架 | React 18 + TypeScript | 用户界面用 React 组件化开发，TypeScript 保证代码质量 |
| UI 组件库 | Ant Design 5 | 蚂蚁金服出品的专业组件库，提供表格、表单、日期选择器等 |
| 构建工具 | Vite 5 | 前端开发服务器和打包工具，速度极快 |
| 数据库 | better-sqlite3 | SQLite 的 Node.js 绑定，数据存在本地文件中 |
| 打包工具 | electron-builder | 一键打包为 Windows .exe 和 Mac .dmg |
| 包管理 | npm | Node.js 的包管理工具 |

---

## ⚠️ 核心开发规则（必须遵守）

### 规则 1：技术方案由用户决定

用户是**非技术背景的小白**。任何涉及以下事项的决策，**必须先列出 2-3 个可选方案，用通俗语言向用户解释每个方案的优缺点，给出推荐方案，然后等待用户决定**，绝不能替用户做决定：

- 技术选型（用什么框架、什么库）
- 架构设计（功能怎么划分、数据怎么流转）
- UI/UX 设计（界面怎么布局、交互怎么设计）
- 功能取舍（做什么功能、不做什么功能）

### 规则 2：用通俗语言沟通

- 避免使用专业术语
- 如果必须使用术语，需要附带解释
- 多用类比和举例来说明技术概念
- 每次解释一个概念时，假设用户是第一次听说

### 规则 3：渐进式开发

- 每个功能模块完成后，先让用户验收确认
- 不要一次性开发所有功能，分批次交付
- 每次交付的内容应该是用户可以直观看到和操作的
- 用户确认当前批次没问题后，再进入下一批次

---

## 支出分类体系

### 一级分类（10 个）

| 一级分类 | 图标 | 二级分类 |
|---------|------|---------|
| 餐饮美食 | 🍜 | 早餐、午餐、晚餐、零食饮料、外出聚餐、食材采购 |
| 交通出行 | 🚗 | 公交地铁、打车/网约车、加油充电、停车费、车辆保养、火车/机票 |
| 购物消费 | 🛒 | 衣服鞋包、数码电子、家居日用、美妆护肤、书籍文具、运动户外 |
| 住房居家 | 🏠 | 房租、水电燃气、物业费、维修装修、宽带话费 |
| 休闲娱乐 | 🎮 | 电影演出、游戏充值、旅游度假、运动健身、视频会员、聚会社交 |
| 医疗健康 | 💊 | 看病变医、药品购买、牙科口腔、体检、保健品 |
| 教育学习 | 📚 | 学费、书籍资料、网课、培训考试、文具耗材 |
| 金融理财 | 💰 | 保险、投资亏损、贷款还款、手续费 |
| 家庭生活 | 👨‍👩‍👧 | 育儿支出、宠物花销、家居用品、人情红包 |
| 其他支出 | 📦 | 快递物流、其他杂项 |

---

## 项目结构

```
黑马记账APP/
├── CLAUDE.md                 # 项目文档（本文件）
├── package.json              # 项目配置和依赖
├── electron-builder.yml      # 打包配置
├── tsconfig.json             # TypeScript 主配置
├── tsconfig.node.json        # 主进程 TypeScript 配置
├── tsconfig.web.json         # 渲染进程 TypeScript 配置
├── vite.config.ts            # Vite 构建配置
├── resources/                # 应用图标等资源
├── src/
│   ├── main/                 # Electron 主进程
│   │   ├── index.ts          # 主进程入口，创建窗口
│   │   └── database.ts       # SQLite 数据库初始化与操作
│   ├── preload/              # 预加载脚本（主进程与页面之间的安全桥梁）
│   │   └── index.ts          # 暴露给页面的 API
│   └── renderer/             # React 前端（用户界面）
│       ├── index.html        # 入口 HTML
│       ├── main.tsx          # React 入口
│       ├── App.tsx           # 根组件
│       ├── components/       # 可复用组件
│       ├── pages/            # 页面组件
│       └── styles/           # 样式文件
```

---

## 数据库设计

### 表：expenses（支出记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| amount | REAL | 金额（人民币，单位：元） |
| category_l1 | TEXT | 一级分类 |
| category_l2 | TEXT | 二级分类 |
| note | TEXT | 备注（可选） |
| date | TEXT | 日期（格式：YYYY-MM-DD） |
| created_at | TEXT | 创建时间 |

---

## 功能规划

### v1.0 — 基础功能（首批开发）

- [ ] 添加支出记录（金额、分类、日期、备注）
- [ ] 二级分类联动选择（选一级→自动显示对应二级）
- [ ] 支出记录列表展示
- [ ] 删除支出记录
- [ ] 编辑支出记录
- [ ] 按月份筛选查看

### v1.1 — 统计功能（第二批开发）

- [ ] 月度支出汇总
- [ ] 按分类统计（饼图）
- [ ] 月度趋势（折线图）

### v1.2 — 增强功能

- [ ] 数据导出为 CSV/Excel
- [ ] 月度预算设置
- [ ] 搜索功能

---

## 开发笔记

- 项目创建日期：2026-07-11
- 数据库文件位置：应用数据目录下的 `heima-accounting.db`
- 应用打包后，数据库文件存储在用户数据目录中（不会因应用更新而丢失）
